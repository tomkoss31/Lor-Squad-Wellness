// Chantier Paramètres Admin (2026-04-23) — placeholder étoffé commit 5.
import { Card } from "../ui/Card";

export function TransfertsTab() {
  return (
    <Card className="space-y-3">
      <p className="eyebrow-label">Transferts</p>
      <p style={{ fontSize: 14, color: "var(--ls-text-muted)" }}>
        Réassigner un distributeur à un nouveau parrain (en construction).
      </p>
    </Card>
  );
}
