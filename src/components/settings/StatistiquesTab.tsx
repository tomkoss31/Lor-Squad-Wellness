// Chantier Paramètres Admin (2026-04-23) — placeholder étoffé commit 6.
import { Card } from "../ui/Card";

export function StatistiquesTab() {
  return (
    <Card className="space-y-3">
      <p className="eyebrow-label">Statistiques</p>
      <p style={{ fontSize: 14, color: "var(--ls-text-muted)" }}>
        KPIs équipe et activité (en construction).
      </p>
    </Card>
  );
}
