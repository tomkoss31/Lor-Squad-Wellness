// Chantier Paramètres Admin (2026-04-23) — placeholder étoffé commit 4.
import { Card } from "../ui/Card";

export function EquipeTab() {
  return (
    <Card className="space-y-3">
      <p className="eyebrow-label">Équipe</p>
      <p style={{ fontSize: 14, color: "var(--ls-text-muted)" }}>
        Liste des distributeurs de ta ligne (en construction).
      </p>
    </Card>
  );
}
